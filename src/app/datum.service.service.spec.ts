/*import { TestBed } from '@angular/core/testing';

import { Datum.ServiceService } from './services/datum.service.service';

describe('Datum.ServiceService', () => {
  let service: Datum.ServiceService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Datum.ServiceService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
